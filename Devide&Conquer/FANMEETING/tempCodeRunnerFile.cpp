vector<int> multiple(vector<int> a, vector<int> b)
// {
//     int len = a.size();
//     if (len != b.size())
//     {
//         return 0;
//     }
//     if (a.size() == 1)
//     {
//         return a[0] * b[0];
//     }
//     vector<int> head = multiple(slice(a.begin(), a.begin() + (len / 2)), slice(b.begin(), b.begin() + () len / 2));
//     vector<int> tail = multiple(slice(a.begin() + (len / 2), a.end()), slice(b.begin() + (len / 2), b.end()));
// }

// int hugs(string members, string fans)
// {
//     int memLen = members.length();
//     int fanLen = fans.length();

//     vector<int> membersToBinary = stringToBinary(members);
//     vector<int> fansToBinary = stringToBinary(fans);
// }